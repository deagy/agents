# Archived Orchestration Examples

This directory contains historical examples and generated dispatch artifacts used to exercise the agent suite. They are not active product guidance.

Example packages may preserve original task identifiers, paths, and run records so selector behavior and review handoffs remain reproducible. Do not treat archived examples as current architecture decisions, production approval, or required project structure.
